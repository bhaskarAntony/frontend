
const sum = (x, y) =>{
    return x + y;
}

const mul = (x, y) => {
    console.log('product = ', x * y);
}
module.exports = {sum, mul} //we are exporting more than one method thats why we are using curly brackets here and it is a constant exports